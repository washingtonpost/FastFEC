[case]
description = The source file has fewer delimiters in data rows than in the
    header row. The output csvs should have trailing commas added so
    that there are always the same number of delimiters in each row.
    https://github.com/washingtonpost/FastFEC/issues/24
x_fail=True
x_fail_reason = Have not yet fixed https://github.com/washingtonpost/FastFEC/issues/24
