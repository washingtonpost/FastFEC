[case]
description = The source file has more delimiters in data rows than in the
    header row. We should truncate the later fields so
    that there are always the same number of delimiters in each row.
    https://github.com/washingtonpost/FastFEC/issues/24
